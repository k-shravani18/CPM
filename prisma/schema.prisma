generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model Instances {
  id         String @id @default(auto()) @map("_id") @db.ObjectId
  cloud      String
  time_stamp String
  instances  Json?
}
